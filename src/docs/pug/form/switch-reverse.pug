extends ../example.pug

block head_title
  +example_head_title('form', 'switch_reverse')

block meta_description
  +example_meta_description('form', 'switch_reverse')

block breadcrumb
  +example_breadcrumb('form')

block example_description
  +example_description('form', 'switch_reverse')

block example_result
  .custom-control.custom-switch.custom-switch-reverse
    input.custom-control-input(type="checkbox" id="customSwitch1")
    label.custom-control-label(for="customSwitch1") Toggle this switch element

block example_code
  :encode-pug
    | <!-- Source -->
    .custom-control.custom-switch.custom-switch-reverse
      input.custom-control-input(type="checkbox" id="customSwitch1")
      label.custom-control-label(for="customSwitch1") Toggle this switch element
