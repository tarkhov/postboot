extends ../example-responsive.pug

block example_description
  +example_description('navbar', 'size')

block example_result
  - for(var size = 1; size <= 5; size++)
    +navbar_size(size)

block example_code
  :encode-pug
    | <!-- Source -->
    - for(var size = 1; size <= 5; size++)
      nav.navbar(class='navbar-' + size)

block responsive_description
  h2.h3.fw-400 Responsive navbar sizes

block responsive_code
  :encode-pug
    | <!-- Source -->
    each breakpoint in ['sm', 'md', 'lg', 'xl']
      - for(var size = 1; size <= 5; size++)
        nav.navbar(class='navbar-' + breakpoint + '-' + size)
      ="\n"
