extends ../example.pug

block head_title
  +example_head_title('font', 'size')

block meta_description
  +example_meta_description('font', 'size')

block breadcrumb
  +example_breadcrumb('font')

block example_description
  +example_description('font', 'size')

block example_result
  - for(var size = 1; size <= 12; size++)
    +font_size('Font size ' + size, size)

block example_code
  :encode-pug
    | <!-- Source -->
    - for(var size = 1; size <= 12; size++)
      p(class='font-' + size)= 'Font size ' + size

block example_after
  #responsive.pt-3
    h2.h4.font-400.mb-3 Responsive
    .card.card-body.bg-card-cap
      +example_code
        :encode-pug
          | <!-- Responsive -->
          each breakpoint in ['sm', 'md', 'lg', 'xl']
            - for(var size = 1; size <= 12; size++)
              p(class='font-' + breakpoint + '-' + size)
            ="\n"
  .clearfix

block sidebar_right_items_after
  li.nav-item
    a.nav-link(href="#responsive") Responsive
