extends ../example.pug

block head_title
  +example_head_title('nav', 'tabs_bottom')

block meta_description
  +example_meta_description('nav', 'tabs_bottom')

block breadcrumb
  +example_breadcrumb('nav')

block example_description
  +example_description('nav', 'tabs_bottom')

block example_result
  +nav(['nav-tabs', 'nav-tabs-bottom'])

block example_code
  :encode-pug
    | <!-- Source -->
    ul.nav.nav-tabs.nav-tabs-bottom
