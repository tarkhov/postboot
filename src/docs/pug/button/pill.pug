extends ../example.pug

block head_title
  +example_head_title('button', 'pill')

block meta_description
  +example_meta_description('button', 'pill')

block breadcrumb
  +example_breadcrumb('button')

block example_description
  +example_description('button', 'pill')

block example_result
  +button('Ellipse', ['btn-primary', 'btn-pill'])

block example_code
  :encode-pug
    | <!-- Source -->
    button.btn.btn-primary.btn-pill(type='button') Ellipse
