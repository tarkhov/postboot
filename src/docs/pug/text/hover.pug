extends ../example.pug

block head_title
  +example_head_title('text', 'hover')

block meta_description
  +example_meta_description('text', 'hover')

block breadcrumb
  +example_breadcrumb('text')

block example_description
  +example_description('text', 'hover')

block example_result
  each text, variant in colors
    +text_hover(text, variant)

block example_code
  :encode-pug
    | <!-- Source -->
    each text, variant in colors
      div(class='text-hover-' + variant)= text
