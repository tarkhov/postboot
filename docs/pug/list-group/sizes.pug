extends ../example.pug

block example_link
  a.text-default(href="#list-group-sizes") Sizes

block example_result
  .row
    each size in ['xs', 'sm', 'lg']
      .col-md-4
        +list_group_variant(size)

block example_code
  :encode-pug
    | <!-- Source -->
    each size in ['xs', 'sm', 'lg']
      .list-group(class='list-group-' + size)
