extends ../example-html.pug

block example_title
  h4.fw-400 Responsive menu size
  span.badge.badge-success.text-uppercase.mb-3 New

block example_code
  :encode-pug
    | <!-- Source -->
    each breakpoint in ['sm', 'md', 'lg', 'xl']
      - for(var size = 1; size <= 5; size++)
        .dropdown-menu(class='dropdown-menu-' + breakpoint + '-' + size)
      ="\n"
