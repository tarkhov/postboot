extends ../example.pug

block example_title
  h3.fw-400 Widths

block example_result
  - for(var width = 1; width <= 5; width++)
    +border_width(width)

block example_code
  :encode-pug
    | <!-- Source -->
    - for(var width = 1; width <= 5; width++)
      p(class='border-' + width)= width + 'px'
      each position in ['top', 'right', 'bottom', 'left']
        p(class='border-' + position + '-' + width)= width + 'px'
      ="\n"
