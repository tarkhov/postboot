extends ../example.pug

block example_title
  h3.fw-400 Sizes

block example_result
  each size in ['sm', 'lg']
    +navbar_size(size)

block example_code
  :encode-pug
    | <!-- Source -->
    each size in ['sm', 'lg']
      nav.navbar(class='navbar-' + size)
