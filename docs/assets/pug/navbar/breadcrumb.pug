extends ../example.pug

block example_title
  h1.font-weight-normal Sizes

block example_result
  each size in ['sm', 'lg']
    +navbar_size(size)

block example_code
  :encode-pug
    | <!-- Source -->
    each size in ['sm', 'lg']
      nav.navbar.navbar-expand-sm.navbar-dark.bg-dark(class='navbar-' + size)
