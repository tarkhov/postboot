extends ../example.pug

block example_title
  h2.font-weight-normal Glass

block example_body
  .card-body.bg-dark.bg-striped
    each text, variant in colors
      +text_glass(text, variant)

block example_code
  :encode-pug
    | <!-- Source -->
    each text, variant in colors
      div(class='text-glass-' + variant)= text
