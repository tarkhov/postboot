extends ../example-html.pug

block example_title
  h4.fw-400 Responsive card sizes

block example_code
  :encode-pug
    | <!-- Source -->
    each breakpoint in ['sm', 'md', 'lg', 'xl']
      - for(var size = 1; size <= 3; size++)
        .card(class="card-" + breakpoint + '-' + size)
      ="\n"
