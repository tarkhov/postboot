extends ../example.pug

block example_title
  h3.fw-400 Opacity

block example_body
  .card-body.bg-dark.bg-striped
    +badge_variant('Black', 'black-50')

block example_code
  :encode-pug
    | <!-- Source -->
    - for(var i = 1; i <= 9; i++)
      .badge(class='badge-black-' + i * 10)
