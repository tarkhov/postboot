extends ../example.pug

block example_title
  h3.fw-400 Sizes

block example_result
  - for(var size = 1; size <= 4; size++)
    +pagination_size(size)

block example_code
  :encode-pug
    | <!-- Source -->
    - for(var size = 1; size <= 4; size++)
      ol.pagination(class='pagination-' + size)
