extends ../example.pug

block example_title
  h3.font-weight-normal Alpha

block example_body
  .card-body.bg-dark.bg-striped
    each text, variant in colors
      +background_alpha(text, variant)

block example_code
  :encode-pug
    | <!-- Source -->
    each text, variant in colors
      p.text-white(class='bg-' + variant + '-50')= text
