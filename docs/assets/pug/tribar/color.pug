extends ../example.pug

block example_title
  h3.fw-400 Color

block example_result
  each text, variant in colors
    .d-inline-block.mr-1.mb-2
      .tribar(class='tribar-' + variant)

block example_code
  :encode-pug
    | <!-- Source -->
    each text, variant in colors
      .tribar(class='tribar-' + variant)
